import { strata } from "Strata"
import {
  a,
  text,
  href,
  footer,
  className,
  span
} from "MadUI"
import type {Element} from "MadUI"
import type { State } from "@/client/State"

stratum = strata("footer")

Footer :: State -> Element State
export Footer = (state) => 
  <footer className={stratum.e("")}>
    <span className={stratum.e("power")}>
      This site powered by <a href="https://github.com/madlib-lang/madlib" className={stratum.e("link")}>Madlib</a>
    </span>
  </footer>
