import type { Element } from "MadUI"

import type { State } from "@client/State"

import { page } from "@client/views/Page"

NotFound :: State -> Element State
export NotFound =
  page(
    "Not found",
    "The requested page could not be found"
  )

export slug = "*"
export view = NotFound
