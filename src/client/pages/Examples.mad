import type { Element } from "MadUI"
import {
  className,
  div,
  h1,
  main,
  text
} from "MadUI"
import { namedPageWithChildren } from "@client/RenderNamedPage"

import type { State } from "@client/State"

export Examples = namedPageWithChildren("dope", "examples")
